" This file defines global variables that might be used by other config files.
" Name this file 00-*.vim so that it is the first one sourced in the config
" directory.

"-----------------------------------------------
" Define global variables that will be used by multiple plugins
let g:diagnostic_error_symbol = '✗'
let g:diagnostic_warning_symbol = ''
let g:diagnostic_info_symbol = ''
let g:diagnostic_hint_symbol = ''
let g:diagnostic_ok_symbol = '✓'

